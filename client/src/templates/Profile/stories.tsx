import { Story, Meta } from '@storybook/react'
import ProfileTemplate from '.'

export default {
  title: 'ProfileTemplate',
  component: ProfileTemplate
} as Meta

export const Default: Story = () => <ProfileTemplate />
